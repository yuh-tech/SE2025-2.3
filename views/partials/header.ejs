<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title><%= typeof title !== "undefined" ? title : "Sunshine Boutique" %></title>
  <link rel="stylesheet" href="/style.css" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
</head>

<body>
  <header class="header">
    <div class="logo">
      <a href="/home">Sunshine Boutique</a>
    </div>

    <nav class="nav">
      <a href="/products">Sản phẩm</a>
      <a href="/cart" class="cart-link">
        Giỏ hàng
        <span class="cart-badge"><%= cart.length %></span>
      </a>

      <% if (currentUser) { %>
        <span class="user-name">Hi, <%= currentUser.displayName || currentUser.username %></span>
        <form action="/logout" method="POST" class="logout-form">
          <button type="submit" class="logout-btn">Đăng xuất</button>
        </form>
      <% } else { %>
        <a href="/login" class="login-btn">Đăng nhập</a>
      <% } %>
    </nav>
  </header>

  <main class="main-container">
