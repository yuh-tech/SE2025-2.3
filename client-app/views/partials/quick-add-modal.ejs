<!-- views/partials/quick-add-modal.ejs -->
<div id="quickAddModal" class="modal-overlay" style="display:none;">
  <div class="modal-content">
    <span class="modal-close">×</span>

    <div class="modal-product">
      <img id="modalImg" src="" alt="">
      <div class="modal-info">
        <h3 id="modalName"></h3>
        <p class="modal-price" id="modalPrice"></p>
      </div>
    </div>

    <div class="modal-options">
      <div class="option-group">
        <p>Màu sắc</p>
        <div id="colorOptions" class="option-list"></div>
      </div>

      <div class="option-group">
        <p>Kích cỡ</p>
        <div id="sizeOptions" class="option-list"></div>
      </div>

      <p class="modal-warning" style="display:none;color:red;">
        Vui lòng chọn đủ màu và size
      </p>
    </div>

    <div class="modal-actions">
      <button id="modalAddBtn" class="modal-add-btn" disabled>
        Thêm vào giỏ
      </button>
    </div>
  </div>
</div>


<style>
  .modal-overlay {
    position: fixed; top: 0; left: 0; width: 100%; height: 100%;
    background: rgba(0,0,0,0.6); z-index: 9999; display: flex;
    align-items: center; justify-content: center; backdrop-filter: blur(8px);
  }
  .modal-content {
    background: white; width: 90%; max-width: 420px; border-radius: 20px;
    padding: 25px; position: relative; animation: pop 0.3s ease;
  }
  @keyframes pop { from { transform: scale(0.8); opacity: 0; } to { transform: scale(1); opacity: 1; } }

  .modal-close {
    position: absolute; top: 12px; right: 18px; font-size: 28px;
    cursor: pointer; color: #999; font-weight: bold;
  }
  .modal-product {
    display: flex; gap: 15px; align-items: center; margin-bottom: 20px;
  }
  #modalImg { width: 100px; height: 130px; object-fit: cover; border-radius: 12px; }
  .modal-info h3 { font-size: 18px; margin: 0; }
  .modal-price { font-size: 20px; font-weight: bold; color: #ff6b9d; margin-top: 8px; }

  .modal-options .option { margin: 18px 0; }
  .modal-options label { display: block; margin-bottom: 10px; font-weight: 600; }
  .color-options, .size-options { display: flex; gap: 10px; flex-wrap: wrap; }
  .color-btn, .size-btn {
    padding: 9px 16px; border: 2px solid #ddd; background: white;
    border-radius: 30px; cursor: pointer; font-size: 14px;
  }
  .color-btn.selected, .size-btn.selected {
    background: #ff6b9d; color: white; border-color: #ff6b9d;
  }
  .add-to-cart-btn {
    width: 100%;
    padding: 14px;
    border-radius: 30px;
    border: none;
    font-size: 16px;
    font-weight: 600;
    background: #a8e6cf;
    color: #155d42;
    cursor: pointer;
    transition: 0.2s;
  }

  .add-to-cart-btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  .add-to-cart-btn:not(:disabled):hover {
    background: #88d8b0;
  }

  /* ===== MODAL OVERLAY ===== */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.55);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 9999;
}

/* ===== MODAL BOX ===== */
.modal-content {
  background: #fff;
  width: 480px;
  max-width: 90%;
  border-radius: 16px;
  padding: 20px 22px 24px;
  box-shadow: 0 15px 40px rgba(0,0,0,0.25);
  animation: modalFade 0.25s ease;
}

/* animation nhẹ */
@keyframes modalFade {
  from {
    opacity: 0;
    transform: translateY(10px) scale(0.98);
  }
  to {
    opacity: 1;
    transform: none;
  }
}

/* ===== CLOSE ===== */
.modal-close {
  position: absolute;
  right: 18px;
  top: 14px;
  font-size: 24px;
  cursor: pointer;
  color: #888;
}
.modal-close:hover {
  color: #000;
}

/* ===== PRODUCT INFO ===== */
.modal-product {
  display: flex;
  gap: 16px;
  margin-bottom: 18px;
}

.modal-product img {
  width: 90px;
  height: 110px;
  object-fit: cover;
  border-radius: 10px;
  border: 1px solid #eee;
}

.modal-info h3 {
  margin: 0 0 6px;
  font-size: 18px;
}

.modal-price {
  font-size: 16px;
  font-weight: 600;
  color: #e74c3c;
}

/* ===== OPTIONS ===== */
.option-group {
  margin-bottom: 14px;
}

.option-group p {
  margin: 0 0 6px;
  font-weight: 600;
  font-size: 14px;
}

/* buttons */
.option-list {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.option-btn {
  padding: 6px 14px;
  border-radius: 20px;
  border: 1px solid #ddd;
  background: #fff;
  cursor: pointer;
  font-size: 13px;
  transition: 0.15s;
}

.option-btn:hover {
  border-color: #333;
}

.option-btn.selected {
  background: #111;
  color: #fff;
  border-color: #111;
}

.option-btn:disabled {
  opacity: 0.35;
  cursor: not-allowed;
}

/* ===== WARNING ===== */
.modal-warning {
  margin-top: 8px;
  font-size: 13px;
}

/* ===== ACTIONS ===== */
.modal-actions {
  margin-top: 18px;
}

.btn-primary {
  width: 100%;
  padding: 12px;
  border-radius: 10px;
  border: none;
  font-size: 15px;
  font-weight: 600;
  background: #111;
  color: #fff;
  cursor: pointer;
  transition: 0.2s;
}

.btn-primary:hover {
  background: #000;
}

.btn-primary:disabled {
  background: #ccc;
  cursor: not-allowed;
}
/* ===== Modal Add to Cart Button (sync with theme) ===== */
.modal-actions {
  margin-top: 18px;
}

.modal-add-btn{
  width: 100%;
  height: 48px;
  border: none;
  border-radius: 999px;
  font-size: 15px;
  font-weight: 700;
  letter-spacing: 0.2px;

  /* giống style quick-add-btn ở products */
  background: linear-gradient(135deg, #a8e6cf, #88d8b0);
  color: #155d42;

  cursor: pointer;
  transition: transform 0.2s ease, box-shadow 0.25s ease, filter 0.2s ease;
  box-shadow: 0 10px 22px rgba(136, 216, 176, 0.25);
}

.modal-add-btn:hover:not(:disabled){
  transform: translateY(-2px);
  filter: brightness(1.02);
  box-shadow: 0 14px 28px rgba(136, 216, 176, 0.35);
}

.modal-add-btn:active:not(:disabled){
  transform: translateY(0);
  box-shadow: 0 10px 20px rgba(136, 216, 176, 0.28);
}

.modal-add-btn:disabled{
  background: #eef2f3;
  color: #9aa6ac;
  box-shadow: none;
  cursor: not-allowed;
}

</style>